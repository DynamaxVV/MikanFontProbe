# 切割与输入方案对照

本轮固定基线的 9 张图、27 个已知字符、12 款字体及答案，不换字，不为单题指定算法。旧基线仍保留。

## 已实现

`refine_font_inputs.py` 在原图上沿旧切线附近寻找低墨迹边界。正式版本仅允许向外补边，不能向内切掉原有单元格内容；裁片不做笔画修补。另测试保留灰度覆盖的输入，以及先将模板降采样到源字墨迹尺寸再进行匹配的输入。

输出在 `data/input-ablation-v3/`：逐题、逐字排名，原／新裁片，原图上的新边界。结果不是概率。

| 方案 | 开发样本大类 Top-1 一致数 |
|---|---:|
| 原裁片＋原二值距离 | 6/9 |
| 补边裁片＋原二值距离 | 6/9 |
| 原裁片＋灰度覆盖距离 | 5/9 |
| 补边裁片＋灰度覆盖距离 | 6/9 |
| 补边裁片＋源分辨率模板＋灰度距离 | 6/9 |

灰度路线同时更换输入表达与距离函数（归一化灰度 L1），因此不能把二值／灰度路线之间的变化全归因于灰度。补边前后的同路线、灰度路线内的模板降采样，才是相对可分离的对照。

## 观察与保留意见

- q92 在补边＋灰度两条路线中改为宋体第一；但 q100 在普通灰度路线退化、q115 在源分辨率模板路线退化。
- q72、q96 在所有正式路线中仍然排错。不能认为只改善切割或降低模板分辨率就解决了问题。
- 首次允许边界双向移动时曾得到 7/9，但视觉检查发现 q96 的“万”上横被切掉，**该结果无效，不采纳**。`input-ablation-v1` 作为失败证据保留，v2 是加入禁止缩框后的四路结果，v3 增加模板分辨率对照。
- 新裁片抽看 q92、q96、q100；切割完整性仍未经过完整人工真值验收。只扩框也可能混入邻字，不能作为最终通用切割器。
- 目前没有比原基线稳定更优的替代方案，不把不同方案的正确题目择优拼成“更高准确率”。相近字体及方案间排名不一致应展示多个候选，而非强行给高置信答案。

## 验证与复现

```sh
.venv/bin/python refine_font_inputs.py
.venv/bin/python -m unittest test_adaptive_regions test_typographic_regions test_font_baseline test_refine_font_inputs -v
```

新增回归覆盖内部空隙不能导致缩框、图片边界约束、灰度保留和自匹配。原有基线的二值结果在本轮原样重现。所有测试仅验证实现契约，不等价于实际字体识别质量。

下一阶段应扩充独立核对的同字样本、检查输入是否为精确源字体，并对宋体端点／横竖粗细关系等结构信号做有对照的评分实验。当前仍未引入不确定 OCR，避免增加无法归因的变量。
