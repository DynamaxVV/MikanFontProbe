# 首次新题目验证：冻结评分，不按结果调参

## 结论

12 张此前未用于评分调参的新截图，混合评分 11/12，原评分 10/12。混合评分改对 q129，没有把原本正确的题目改错；两者均错 q99。

提升在新题目上再次出现，是正向证据；但只有一题净增益，**尚不能确认稳定或显著提升，更不能宣称系统准确率为 91.7%**。混合评分可继续作为待验证候选，不将此次结果用作新一轮权重调参依据。

## 选择与冻结

- 圆体：q111、q82、q52、q56。
- 宋体：q98、q69、q42、q9。
- 黑体：q131、q129、q104、q99。
- 每图选 3 个完整且可读字符，共 36 个字、33 个不同字符。字体库仍为同样的 12 款，生成 396 张同字模板。
- 题号与此前 18 张优先实验样本无交集，图片 SHA-256 也没有完全相同项。已视觉浏览 12 张原图，未发现与开发样本相同的整张截图；没有作者／作品元数据，不能保证不同作品、不同作者或不同字体排版来源。
- 在运行评分前用原图和裁片总览核对字符及边界，修正了误取邻字和夹入邻字笔画的裁片；此时没有查看任何新样本评分。
- 样本配置、所有裁片、评分源码与原图指纹写入 `freeze.json` 后才运行评分。评分函数直接复用上一轮，不修改权重、变换范围或聚合规则。运行后不再修改样本。
- q98 是反白字，选取上部暗背景较干净的三个字，预先声明按反白极性翻转；其余使用黑字原图。

这是一组人工挑选可读字的**题目级留出验证**，不是随机抽样、自动切割／OCR 端到端验收或严格跨作品盲测。代理知道题库标签，选字未独立双审；字体文件是否就是漫画原字体仍未证实。

## 配对结果

| 类别 | 原二值评分 | 纯结构评分 | 混合评分 |
|---|---:|---:|---:|
| 圆体 | 4/4 | 4/4 | 4/4 |
| 宋体 | 4/4 | 3/4 | 4/4 |
| 黑体 | 2/4 | 3/4 | 3/4 |
| 合计 | 10/12 | 10/12 | 11/12 |

纯结构评分会将 q42 的宋体排成圆体。混合评分保留原评分在该题的正确结果，同时通过结构信息修正 q129。q99 三种评分都排圆体；本轮不修改它的答案，也不据此声称题库标注错误。

旧开发集的 6/9→7/9 与此次 10/12→11/12 分开报告，不合并成泛化准确率。后续若根据这批错误调参，它就应转为开发集，必须另留新样本。

## 产物与复现

- `data/holdout_samples_v1.json`：预先核对的题号、字符、坐标与极性。
- `data/holdout-v1/crop-review.png`：全部 36 个原图裁片总览。
- `data/holdout-v1/crops/`：原图裁片和匹配输入，反白转换不覆盖原图。
- `data/holdout-v1/freeze.json`：配置／源码／图片／裁片指纹。
- `data/holdout-v1/results.json`：三条路线的逐题候选及距离。
- `data/holdout-v1/templates/catalog.json`：字体指纹、字符覆盖、渲染版本。

新脚本 `evaluate_holdout.py` 提供 prepare 和 evaluate 两步。已完成评分的 v1 禁止重新 prepare，以防悄悄改样本。原基线模板生成脚本仅新增可选 `--samples` 参数，默认仍为旧开发集。

```sh
# 在已有冻结输入上重现评分；任何冻结文件变化都会报错。
.venv/bin/python evaluate_holdout.py evaluate
.venv/bin/python -m unittest test_adaptive_regions test_typographic_regions test_font_baseline test_refine_font_inputs test_structure_baseline test_holdout
```

22 项回归测试通过；验证了样本分组和题号隔离、冻结文件一致性、12 款字体对测试字符的完整覆盖。无新增依赖、外部数据发送或原图／字体文件修改。
