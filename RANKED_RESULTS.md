# 字族 Top-3、质量加权与置信参考

## 本轮交付

- `font_family_policy.py`：按日文字族合并字重；Antique AN+ 和攸望是两个字族但同为黑体答案；RodinHappy L、B、UB 保留少女、方圆、海报三个类别。
- `render_rank_templates.py`：23 个字体文件、14 个候选类别、55 个字符，生成完整同字模板。空目录不参与。
- `rank_font_candidates.py`：不依赖答案的裁片质量分、至多 5 字选取接口、同款字体多字加权、Top-3 字族、参考置信度、移除单字稳定性及复核原因。
- `data/ranked-v1/results.json`：21 张真实样本完整结构化输出。
- `output/pdf/font-recognition-report.pdf`：12 页图文报告，21 题总览、方法、6 个详细原图对照案例、全部 21 题的前三候选附录。

## 置信度含义

输出的 `confidence` 为未校准证据指数，不是 P(正确)，不加百分号，各候选也不合计为 100。候选仍按距离排序，因此第 3 名参考分可能高于第 2 名；两者衡量的内容不同。

分数由拟合程度、输入质量、各字单独支持、移除单字后排名稳定性、第一与第二名差距相乘构成。质量由分辨率、对比度、触边风险估计；不使用题库答案。空白/全黑拒绝，低质量字不入选，不能根据某字不支持当前第一名而删除它。匹配依然要求同一款字体解释全部所选字符，再合并字重。

拒判目前体现为“待复核/库内匹配不足”的告警，不是已校准的缺库检测器。至多 5 字接口已具备，但本批样本每图只有已有的 3 字；没有验证大规模自动选字、自动 OCR 或切分补采。没有输出虚假的高置信自动通过结论。

## 结果及局限

| 评估范围 | 开发9题 | 原留出12题（本轮复用） |
|---|---:|---:|
| 原12款核心字库，等权混合 | 7/9 | 11/12 |
| 原12款核心字库，质量加权 | 7/9 | 11/12 |
| 全23款字库，等权混合 | 6/9 | 8/12 |
| 全23款字库，质量加权 | 6/9 | 8/12 |

质量加权并未在此批样本上证明 Top-1 提升。扩库后相似候选增多，不能把核心字库的 11/12 宣传成全库表现。18/21 题触发复核提示；当前规则偏保守，还需要独立数据校准。

这 12 张旧留出图已被观察，本轮只称回归，不称新盲测。中文大类的一致性不代表精确原字体认证，全部测试仍依赖已知文字与人工核对裁片。

## 复现

```sh
python3 render_rank_templates.py
.venv/bin/python rank_font_candidates.py
python3 build_ranked_report.py
.venv/bin/python -m unittest test_adaptive_regions test_typographic_regions test_font_baseline test_refine_font_inputs test_structure_baseline test_holdout test_ranked_candidates
```

28 项测试通过。原字体/漫画、冻结评分代码和旧留出输入未修改；无新增依赖。PDF 经渲染视觉检查，不仅是文本提取检查。
