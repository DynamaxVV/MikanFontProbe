# 字体结构评分对照 v1

## 结果

同一批 9 张开发截图、27 个已知字符、12 款字体；使用上一轮禁止缩框的补边裁片。没有更改字符或答案，没有按题目指定策略。

| 评分 | 大类 Top-1 一致数 | 相对原二值评分的变化 |
|---|---:|---|
| 原二值重合／距离评分 | 6/9 | 对照组 |
| 局部方向＋投影结构评分 | 7/9 | q72、q96 改对；q125 改错 |
| 原距离与结构各半的混合评分 | 7/9 | q72 改对；原先正确的六题保留 |

**这是开发集结果，不是盲测准确率。** q72 混合评分下宋体与圆体距离仅相差约 0.00069，q96 的圆体与黑体约相差 0.00218，排名很接近；不能按第一名直接输出高置信结论。q92 在这一轮仍排错，上一轮灰度路线的改善没有被强行按题合并进来。

## 实现

新增 `structure_font_baseline.py`，复用字体渲染模板和安全补边后的输入。图像仍保持等比例，不增粗、擦除或修补源笔画。

- 将 64×64 归一化字图分为 4×4 个局部区域，每区统计 8 个无向梯度方向分布；每区独立归一化，降低总墨迹量直接支配距离的问题。
- 加入归一化的横纵墨迹投影，保留笔画布局信息。
- 结构距离固定为 75% 方向分布差异＋25% 投影差异；混合评分固定为原距离与结构距离各半。参数在首轮运行前确定，没有随后按错误题目搜索权重。
- 结构分支独立搜索与原版相同的有限尺度／位移。混合分支在原版最佳对齐上计算两个分量，避免把两个互不相容的对齐得分拼起来。

这不是专门的宋体衬线检测器，也没有证明对笔画粗细完全不敏感。方向特征仍会受分辨率、切割和字体粗细影响。

## 已知字体降质自检

对库内全部 12 款字体各取“今、万、ま”三个字，生成最长边 32px、轻微高斯模糊、JPEG 质量 65 的查询图，再与原模板匹配。三条路线的大类结果均为 **12/12**。

该自检只说明，在这组已知字库、同一渲染来源的退化样本中，匹配流程能恢复大类。它不代表真实截图的字体一定在库中，不覆盖不同排版引擎、描边、压缩链、未知字体或所有低分辨率情况。具体字体是否相同另记在 `controls.json` 的 `exact_font`，不与大类结果混为一谈。

## 文件与复现

- `data/structure-baseline-v1/results.json`：逐字字体得分、整题大类排名和固定参数。
- `data/structure-baseline-v1/controls.json`：已知字体控制实验结果。
- `data/structure-baseline-v1/controls/`：36 张实际降质查询图。
- `test_structure_baseline.py`：新增结构特征契约测试。

```sh
.venv/bin/python structure_font_baseline.py
.venv/bin/python -m unittest test_adaptive_regions test_typographic_regions test_font_baseline test_refine_font_inputs test_structure_baseline
```

19 项测试通过，新脚本与测试语法编译通过。原始字体、题库图片与旧版输出保持不变。

## 下一验证点

混合评分作为新候选保留，不替换原基线。应先扩充按来源去重、尚未用于参数选择的真实截图，评估相近类排序是否稳定；随后再判断是否需要增加字体覆盖或针对衬线／笔画端点的特征。已有 9 张开发图不能继续承担泛化能力证明。
