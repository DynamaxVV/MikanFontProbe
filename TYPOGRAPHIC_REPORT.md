# 字体排版先验切分试验

## 本轮变化

新入口 `typographic_regions.py`，输出 `data/processed/typographic-v3/`。保留旧版与中间试验，不覆盖基线。不做 OCR，也不识别字体名称。

- **列宽与字距**：从候选墨迹投影发现实际列，不把 LLM 给出的框数当成列数；用各列宽度中位数作为主体字宽先验。搜索字距和起始相位，使分界尽量落在少墨迹处，再允许小幅局部调整。
- **白边辅助**：针对黑字浅色描边，先在整张图上找连通域，筛查尺寸、细长背景线、外围浅色支持。之后才限制到粗定位列，避免 ROI 把背景线截断后误认为独立字形。白边只辅助定位，匹配证据仍是原图。
- **证据与不确定性**：每个候选保留原图无重采样裁片、原图坐标和墨迹框。红框表示边界可能穿过墨迹；`possible_mark_or_noise` 提醒小标点或噪声，两者尚未自动区分。所有输出均需复核，不能把无红框视为正确。

## 本轮核验

18 张原有样本已跑完。人工路由仅将 q27 指定为黑字浅描边，其他沿用 adaptive-v3 候选墨迹再进行排版切分；不是已实现自动场景分类。

抽看结果：

- q27：主要字形相较通用分割明显更完整，大量长速度线被排除；仍混入短速度线，产出 17 格，其中有 2 格主要是噪声，而非 17 个正确汉字/假名。不能据此取消原有拒判状态。
- q70：恢复为 7 列，主体切分可读；注音仍可能与主体同框，尚未独立分层。
- q72：3 列，试验输出未标记穿墨切线。
- q91、q33：3 列，规则字距对细圆字有效；标点串不保证一格一个字符。
- q114：原始粗定位只有一个跨列区域，本轮发现 2 列；小假名及省略号仍需复核。
- q65：11 个候选有 9 个穿墨风险标记，说明该例不能直接套用当前规则。白字/反白、描边极性与注音分层尚未实现。

数字是诊断统计，不是识别正确率。无像素级人工真值、未进行本轮独立 Sol 评分，也没有证明在整页或未见漫画上的泛化能力。

## 验证与复现

共 9 个测试通过：原有 5 个区域扩展回归，加上 4 个排版/描边测试（断开笔画的规则排版、跨列粗框、q27 末字锚区与底部速度线、边缘连通背景）。这些测试不能替代人工逐字验收。

```sh
.venv/bin/python typographic_regions.py --outlined-ids 27 --version typographic-v3
.venv/bin/python -m unittest test_adaptive_regions test_typographic_regions -v
```

示例：

- [q27 切格](data/processed/typographic-v3/q27-segments.png)
- [q27 墨迹叠加](data/processed/typographic-v3/q27-overlay.png)
- [q70 切格](data/processed/typographic-v3/q70-segments.png)
- [q91 切格](data/processed/typographic-v3/q91-segments.png)
- [结构化结果](data/processed/typographic-v3/results.json)

## 边界

目前的共享宽度以整张局部截图为单位，不适合混合字号、多个不同字体块。无横排支持、自动极性路由、独立注音层或字体文件匹配。白边支持也不是充分证据：白底上的短背景线同样可能满足条件。下一步应按文字块估计尺度，并结合格内主体字形与侧边注音关系区分噪声，不能靠不断增加全局阈值解决。
